{% extends 'base.html.twig' %}

{% block title %}Admin{% endblock %}

{% block body %}
    <h1>Admin</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ admin.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ admin.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ admin.prenom }}</td>
            </tr>
            <tr>
                <th>Marque</th>
                <td>{{ admin.marque }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ admin.mail }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_admin_index') }}">back to list</a>

    <a href="{{ path('app_admin_admin_edit', {'id': admin.id}) }}">edit</a>

    {{ include('admin/admin/_delete_form.html.twig') }}
{% endblock %}
