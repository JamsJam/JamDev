{% extends 'base.html.twig' %}

{% block title %}Mail{% endblock %}

{% block body %}
    <h1>Mail</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mail.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ mail.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ mail.prenom }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ mail.mail }}</td>
            </tr>
            <tr>
                <th>Sujet</th>
                <td>{{ mail.sujet }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ mail.message }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_mail_index') }}">back to list</a>

    <a href="{{ path('app_admin_mail_edit', {'id': mail.id}) }}">edit</a>

    {{ include('admin/mail/_delete_form.html.twig') }}
{% endblock %}
